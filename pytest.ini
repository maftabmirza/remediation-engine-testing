[pytest]
# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Minimum Python version
minversion = 3.11

# Async support
asyncio_mode = auto

# Core options
addopts = 
    --verbose
    --strict-markers
    --tb=short
    --maxfail=10
    --disable-warnings
    -ra

# Note: Coverage disabled for remote API testing
# Run locally with --cov flag if testing local app instance

#Parallel execution (uncomment to enable)
# -n auto

# Test markers
markers =
    unit: Unit tests for individual components
    integration: Integration tests for component interactions
    api: API endpoint tests
    ui: User interface and browser tests
    security: Security and vulnerability tests
    performance: Performance and load tests
    smoke: Quick smoke tests for sanity checking
    critical: Critical path tests that must pass
    slow: Tests that take longer to execute
    regression: Regression test suite

# Logging
log_cli = true
log_cli_level = INFO
log_file = reports/test.log
log_file_level = DEBUG
log_file_format = %(asctime)s [%(levelname)8s] %(message)s
log_file_date_format = %Y-%m-%d %H:%M:%S

# Timeout (5 minutes default)
timeout = 300
timeout_method = thread

# Warning filters
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning

# Coverage options
[coverage:run]
source = app
omit = 
    */tests/*
    */conftest.py
    */__pycache__/*
    */site-packages/*

[coverage:report]
precision = 2
show_missing = true
skip_covered = false
